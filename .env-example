# Порт, на котором слушает REST API
PORT=4000

# Соль для хеширования паролей (замените на свою случайную строку в .env)
SALT=example-random-salt-change-me-in-local-env

# IP-адрес сервера MongoDB
DB_HOST=127.0.0.1
